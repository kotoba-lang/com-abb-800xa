{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([abb-800xa.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'abb-800xa.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
